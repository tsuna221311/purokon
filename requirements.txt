flask>=3.1,<4
werkzeug>=3.1,<4
pillow>=12.0,<13
numpy>=2.0,<3
scipy>=1.13,<2
svgwrite>=1.4,<2
reportlab>=4.4,<5
rectpack>=0.2,<0.3
shapely>=2.1,<3
anthropic>=0.122,<1
stripe>=11.0,<16
ezdxf>=1.3,<2

# 任意(round8で追加)。SENTRY_DSN環境変数を設定してエラー監視を使う場合のみ
# 必要。未インストールでもアプリ自体は通常通り動く(app.pyが起動時に警告
# ログを出すだけ。README「round8で追加した機能」参照)ため、コメントアウト
# したまま既定の必須依存には含めていない。使う場合はコメントを外すか
# `pip install sentry-sdk` を別途実行すること。
# sentry-sdk>=2.0,<3
